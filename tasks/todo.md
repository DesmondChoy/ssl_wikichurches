# Task Todo

## Current Task

- [x] Mirror the workflow orchestration guidance into `AGENTS.md`
- [x] Mirror the workflow orchestration guidance into `CLAUDE.md`
- [x] Review the documentation diff and run relevant quality checks (`uv run ruff check .`, `uv run mypy`)
- [x] Commit and push the documentation updates

## Review

- Added the requested workflow guidance to both top-level agent instruction files.
- Captured the user correction pattern in `tasks/lessons.md` so future instruction updates check both files upfront.
- Verified the repo quality gate for this session with `uv run ruff check .` and `uv run mypy`.
